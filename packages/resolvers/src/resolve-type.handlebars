{{ toComment description }}
{{#unless @root.config.noNamespaces}}
namespace {{ convert name 'typeNames'}}Resolvers {
{{/unless}}
  interface {{#if @root.config.noNamespaces}}{{ convert name 'typeNames'}}{{/if}}Resolvers {
    __resolveType: {{#if @root.config.noNamespaces}}{{ convert name 'typeNames'}}{{/if}}ResolveType;
  }
  type {{#if @root.config.noNamespaces}}{{ convert name 'typeNames'}}{{/if}}ResolveType<R = {{{ getTypenames this }}}, Parent = {{{ getParentTypes this }}}, Context = {{{ getContext }}}> = TypeResolveFn<R, Parent, Context>;
  
{{#unless @root.config.noNamespaces}}
}
{{/unless}}

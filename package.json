{
  "name": "graphql-codegen-typescript-mocks",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Alex Sandiiarov <sandiiarov@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "prerelease": "yarn build",
    "release": "lerna publish --exact --force-publish=\"*\""
  },
  "devDependencies": {
    "lerna": "^3.10.8"
  }
}
